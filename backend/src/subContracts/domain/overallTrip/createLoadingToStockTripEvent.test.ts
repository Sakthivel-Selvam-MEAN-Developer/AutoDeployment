describe('For an overall trip when trip is created for market vehicle', () => {
    test('when loadingtostock trip is created initial pay should be generated', () => {})
    test('when loadingtostock trip is created and before fueled initial pay should be generated after subtract the fuel amount', () => {})
    test('when loadingtostock trip is created and advance type is 100% initialpay should be generated with 100%', () => {})
    test('when loadingtostock trip is created and advance type is 100% and before fueled initial pay should be generated after subtract the fuel amount from 100%', () => {})
})
