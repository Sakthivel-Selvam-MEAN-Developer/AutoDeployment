import { Request, Response } from 'express'
// import { isEmployeeInOrgUnitHeads } from '../models/orgUnitHeads'

export const employeeLeavesPerOrg = (req: Request, res: Response) => {
    // isEmployeeInOrgUnitHeads(req.params.employeeId).then(({ orgUnitsId }: any) => )
}