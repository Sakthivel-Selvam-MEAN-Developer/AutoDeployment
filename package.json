{
  "name": "wonder-why",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "libs",
    "frontend",
    "backend",
    "apps", "web"
  ],
  "scripts": {
    "u": "yarn workspace web",
    "ui": "yarn workspace web dev",
    "backend": "yarn workspace backend dev",
    "b": "yarn workspace backend",
    "setUpHusky": "husky install",
    "all": "yarn u format; yarn u lint;  yarn u test; yarn b format; yarn b lint;  yarn b testOnce"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
